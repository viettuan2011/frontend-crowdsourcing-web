const routes = {
    home: '/',
    action: '/action',
    training: '/training',
    login: '/login',
    register: '/register',
    logout: '/logout',
    actiontest: '/actiontest',
};

export default routes;
